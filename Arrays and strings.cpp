#include <stdio.h>
#include <string.h>

int main()
{
	/*int point[]={0,0,1,0,0,0};
	int numbers[2000]={245};
	int x;
	x = point[2];
	
	printf("%d", x);*/
	
	/*char y;
	int z = 9;
	char point[6] = { 1, 2, 3, 4, 5, 6 };
	//examples of accessing outside the array. A compile error is not always raised
	y = point[15];
	y = point[-4];
	y = point[z];
	printf(" %c%d", y, z);*/
	
	/*
	int ix;
	short anArray[]= { 3, 6, 9, 12, 15 };
	int size = sizeof(anArray)/sizeof(short);
 
	for (ix=0; ix< size; ++ix) {
  		printf("%d", anArray[ix] );
		}
	*/
	
	/*
	char two_d[3][5];
	char ch;
	ch = two_d[2][4];
	*/
	//or 
	
	/*
    int two_d[2][3] = {
        { 5, 2, 1 },
        { 6, 7, 8 }
    };

    // Get the number of rows and columns
    int rows = 2;
    int cols = 3;

    // Loop through each row
    for (int i = 0; i < rows; i++) {
        // Loop through each column in the current row
        for (int j = 0; j < cols; j++) {
            printf("%d ", two_d[i][j]); // Print element at row i, column j
        }
        printf("\n"); // Newline after each row
    }
	*/
	
	/*
	int a[100];
	int i = 0;
	if (a[i]==i[a])
	{
  		printf("Hello world!\n");
	}
	*/
	
	/*
	char stringA[11] = "Merkkijono";
	char stringB[58] = "This is a very, very long "
                "string that requires two lines.";
                
                
	printf("%s\n%s", stringA, stringB);
	*/
	
	
	
	return 0;
}